<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Gradiente vertical para toast de información -->
    <gradient
        android:type="linear"
        android:angle="90"
        android:startColor="@color/toast_info_start"
        android:endColor="@color/toast_info_end" />

    <!-- Bordes redondeados -->
    <corners android:radius="12dp" />
</shape>
