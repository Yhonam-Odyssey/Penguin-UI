<?xml version="1.0" encoding="utf-8"?>
<!-- Layout para Bottom Sheet Dialog - Menú de acciones deslizante -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/bottomSheetContainer"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="@drawable/bg_bottom_sheet"
    android:paddingBottom="8dp">

    <!-- Handle superior (para arrastrar) -->
    <View
        android:id="@+id/bottomSheetHandle"
        android:layout_width="40dp"
        android:layout_height="4dp"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="12dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/bg_bottom_sheet_handle" />

    <!-- Título del bottom sheet -->
    <TextView
        android:id="@+id/bottomSheetTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="8dp"
        android:text="Opciones"
        android:textColor="@color/bottom_sheet_text_primary"
        android:textSize="18sp"
        android:textStyle="bold"
        android:fontFamily="@font/comic_neu_bold"
        android:gravity="center"
        android:maxLines="2"
        android:ellipsize="end" />

    <!-- Divider -->
    <View
        android:layout_width="match_parent"
        android:layout_height="1dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginBottom="8dp"
        android:background="@color/bottom_sheet_item_ripple"
        android:alpha="0.3" />

    <!-- Lista de items (RecyclerView para mejor performance) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/bottomSheetRecyclerView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:clipToPadding="false"
        android:paddingTop="4dp"
        android:paddingBottom="4dp" />

    <!-- Botón de cancelar (opcional) -->
    <Button
        android:id="@+id/btnCancel"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="16dp"
        android:layout_marginTop="8dp"
        android:layout_marginBottom="8dp"
        android:background="@drawable/bg_bottom_sheet_cancel_button"
        android:text="Cancelar"
        android:textColor="@color/bottom_sheet_text_secondary"
        android:textSize="14sp"
        android:textStyle="bold"
        android:fontFamily="@font/comic_neu_bold"
        android:textAllCaps="false"
        android:visibility="visible" />

</LinearLayout>
