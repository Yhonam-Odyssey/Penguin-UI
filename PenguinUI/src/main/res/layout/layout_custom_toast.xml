<?xml version="1.0" encoding="utf-8"?>
<!-- Layout para Toast Personalizado - Estilo Web Alerts -->
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/toastContainer"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="16dp"
    app:cardBackgroundColor="@color/toast_bg_dark"
    app:cardCornerRadius="12dp"
    app:cardElevation="8dp"
    app:cardUseCompatPadding="true">

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:minWidth="300dp"
        android:padding="0dp">

        <!-- Barra lateral de acento de color -->
        <View
            android:id="@+id/toastAccentBar"
            android:layout_width="6dp"
            android:layout_height="match_parent"
            android:background="@drawable/bg_toast_accent_bar" />

        <!-- Contenido del toast -->
        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:paddingStart="16dp"
            android:paddingTop="14dp"
            android:paddingEnd="20dp"
            android:paddingBottom="14dp"
            android:gravity="center_vertical">

            <!-- Ícono del toast -->
            <ImageView
                android:id="@+id/toastIcon"
                android:layout_width="28dp"
                android:layout_height="28dp"
                android:layout_marginEnd="12dp"
                android:contentDescription="Toast icon"
                android:src="@drawable/ic_toast_success" />

            <!-- Contenedor de texto -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_weight="1">

                <!-- Título del toast -->
                <TextView
                    android:id="@+id/toastTitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Éxito"
                    android:textColor="@color/toast_text_title"
                    android:textSize="15sp"
                    android:textStyle="bold"
                    android:fontFamily="@font/comic_neu_bold"
                    android:maxLines="1"
                    android:ellipsize="end" />

                <!-- Mensaje del toast -->
                <TextView
                    android:id="@+id/toastMessage"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="2dp"
                    android:text="Operación realizada correctamente"
                    android:textColor="@color/toast_text_message"
                    android:textSize="13sp"
                    android:maxLines="2"
                    android:ellipsize="end"
                    android:fontFamily="sans-serif" />

            </LinearLayout>

        </LinearLayout>

    </LinearLayout>

</androidx.cardview.widget.CardView>
